// Service disabled - AI features removed
// 该服务已禁用，此前用于 AI 代码生成功能
// 目前仅保留占位符以防止导入错误
export const generateCodeFromPrompt = async (prompt: string, currentCode: string): Promise<string> => {
    return currentCode;
};